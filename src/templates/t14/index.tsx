import { BrandConfig } from '@/lib/types'

export function Template14({ brand }: { brand: BrandConfig }) {
  return (
    <div>
      <h1>Pirates Wheel Template Preview</h1>
      <p>This template uses server-side rendering for the wheel functionality.</p>
    </div>
  )
}
